# 003 - 数据库服务架构重构

## 📋 概述

本规范文档描述了 `w2-db-query` 项目的数据库服务架构重构设计，旨在实现符合 SOLID 原则、特别是开闭原则（Open-Closed Principle）的架构，使得未来添加新数据库类型时无需修改现有代码。

## 🎯 重构目标

### 核心目标

**当添加新数据库时，开发者应该只需要：**

1. ✅ 在 `DatabaseType` 枚举中添加一个变体（1 行代码）
2. ✅ 创建一个服务文件实现 `DatabaseService` trait（约 300-500 行）
3. ✅ 在 Factory 中注册服务（3 行代码）
4. ✅ 更新模块导出（1 行代码）

**不应该需要修改：**
- ❌ 命令处理器（`commands/*.rs`）
- ❌ 其他数据库服务
- ❌ 类型转换逻辑
- ❌ 错误处理代码

### 架构原则

| 原则 | 说明 |
|------|------|
| **S** - 单一职责 | 每个服务只负责一个数据库类型 |
| **O** - 开闭原则 | 通过 Trait 实现扩展，无需修改现有代码 |
| **L** - 里氏替换 | 任何 DatabaseService 实现都可替换 |
| **I** - 接口隔离 | Trait 方法精简，不强迫实现不需要的方法 |
| **D** - 依赖倒置 | 命令层依赖 DatabaseService 抽象，而非具体实现 |

## 📁 文档目录

```
003-arch-improve/
├── README.md                    # 本文档 - 规范概述
├── architecture-design.md       # 详细架构设计文档
├── migration-guide.md           # 迁移实施指南
├── examples/                    # 代码示例
│   ├── trait.rs                # DatabaseService trait 定义
│   ├── factory.rs              # 工厂模式实现
│   ├── postgres_impl.rs        # PostgreSQL 实现示例
│   └── mysql_impl.rs           # MySQL 实现示例
└── validation.md               # 验证清单
```

## 📊 架构对比

### 修改点对比

| 操作 | 当前架构 | 新架构 |
|------|---------|--------|
| 添加新数据库 | 修改 8 处 | 修改 4 处，零破坏性修改 |
| 修改查询逻辑 | 修改所有命令 | 修改一个 trait 方法 |
| 添加新方法 | 修改所有服务 | 修改 trait + 所有实现 |
| 测试新数据库 | 需要修改现有测试 | 独立测试 |

### 代码量对比

| 组件 | 当前 | 新架构 | 变化 |
|------|------|--------|------|
| Trait 定义 | 0 | ~150 行 | +150 |
| Factory | 0 | ~100 行 | +100 |
| PostgreSQL 实现 | ~400 行 | ~450 行 | +50 |
| MySQL 实现 | ~350 行 | ~400 行 | +50 |
| 命令处理器 | ~800 行 | ~400 行 | -400 |
| **总计** | **~1550 行** | **~1400 行** | **-150 行 (-10%)** |

## 🚀 快速开始

### 1. 阅读架构设计

首先阅读 [architecture-design.md](./architecture-design.md) 了解：
- 当前架构的问题分析
- 新架构的设计方案
- 核心组件和接口定义
- 代码示例

### 2. 查看迁移指南

参考 [migration-guide.md](./migration-guide.md) 了解：
- 分阶段迁移计划
- 每个阶段的具体任务
- 回滚策略
- 测试策略

### 3. 查看代码示例

在 [examples/](./examples/) 目录中查看：
- Trait 定义示例
- Factory 实现示例
- PostgreSQL/MySQL 实现示例
- 重构后的命令处理器示例

### 4. 验证实施

使用 [validation.md](./validation.md) 中的清单验证：
- 实现前的准备工作
- 实施过程中的检查点
- 实施后的验证步骤

## 🏗️ 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                      Tauri Commands Layer                    │
│  (commands/database.rs, commands/query.rs, etc.)             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ depends on
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   DatabaseServiceFactory                     │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  HashMap<DatabaseType, Arc<dyn DatabaseService>>    │    │
│  └─────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ creates
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  DatabaseService Trait                       │
│  - connect()                                                │
│  - test_connection()                                        │
│  - execute_query()                                          │
│  - extract_metadata()                                       │
│  - convert_row_to_json()                                    │
│  - get_sql_dialect()                                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ implemented by
                         ▼
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  PostgreSQL  │ │    MySQL     │ │   SQLite     │
│   Service    │ │   Service    │ │   Service    │
│              │ │              │ │   (Future)   │
└──────────────┘ └──────────────┘ └──────────────┘
```

## 📖 相关文档

- [W2 项目说明](../README.md)
- [001 数据库查询功能](../001-db-query/)
- [002 MySQL 支持](../002-mysql-support/)

## 🤝 贡献指南

如果您想改进此架构设计：

1. Fork 项目仓库
2. 创建特性分支：`git checkout -b feature/003-arch-improve`
3. 提交更改：`git commit -m 'Improve database architecture'`
4. 推送到分支：`git push origin feature/003-arch-improve`
5. 创建 Pull Request

## 📝 变更日志

| 版本 | 日期 | 更改 |
|------|------|------|
| 1.0.0 | 2026-01-26 | 初始架构设计文档 |

---

**规范版本**：1.0.0
**创建日期**：2026-01-26
**状态**：设计阶段
**负责人**：开发团队
